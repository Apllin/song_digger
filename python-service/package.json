{
  "name": "@song-digger/python-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "dev": ".venv/bin/uvicorn app.main:app --reload",
    "test": ".venv/bin/pytest"
  }
}
